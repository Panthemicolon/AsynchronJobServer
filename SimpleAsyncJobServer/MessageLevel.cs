namespace SimpleAsyncJobServer
{
    public enum MessageLevel
    {
        Critical = 10, // Critical Error
        Error = 20,
        Warning = 30,
        Information = 50,
        Verbose = 80,
        Debug = 100
    }
}